import {StyleSheet} from 'react-native';
import {colors} from '../../styles/colors.ts';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: colors.typography1100,
  },
});
